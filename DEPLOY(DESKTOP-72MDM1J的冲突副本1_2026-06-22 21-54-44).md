# 单词突击007 — 服务器部署文档

> 本文档包含完整的服务器配置信息，仅限私有仓库。

---

## 1. 服务器信息

| 项 | 值 |
|---|---|
| 项目名 | 单词突击007 / 中萱文化 |
| 域名 | `https://zxsz007.cn` (主), `https://www.zxsz007.cn` (跳主) |
| 后端端口 | 3001 (生产) / 80 (本地) |
| 公开地址 | `https://zxsz007.cn` |
| 后台管理 | `https://zxsz007.cn/admin` |
| 服务器 IP | `165.99.43.241` |
| 服务器用户 | `root` |
| 应用目录 | `/root/danci007web` |
| 技术栈 | Vue 3 + Vite + vue-router + TypeScript (前端) / Node.js + Express + better-sqlite3 (后端) |
| 数据库 | SQLite (WAL 模式)，文件 `backend/data/danci007.db` |
| 进程管理 | PM2 (`danci007web`) |
| 反向代理 | nginx 1.18 + Let's Encrypt SSL |

---

## 2. 部署方式

### 2.1 自动部署（推荐）

```bash
# 本地修改 → 推送到私有仓库
git push-all

# 服务器自动拉取
ssh root@165.99.43.241
cd /root/danci007web
git pull origin main
pm2 restart danci007web --update-env
```

### 2.2 使用部署脚本

```bash
# 在服务器上执行
cd /root/danci007web
bash scripts/deploy.sh update
```

---

## 3. 服务器配置

### 3.1 Nginx 配置

```nginx
server {
    listen 443 ssl http2;
    server_name zxsz007.cn www.zxsz007.cn;

    ssl_certificate /etc/letsencrypt/live/zxsz007.cn/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/zxsz007.cn/privkey.pem;

    location / {
        root /root/danci007web/dist;
        try_files $uri $uri/ /index.html;
    }

    location /api {
        proxy_pass http://127.0.0.1:3001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /uploads {
        proxy_pass http://127.0.0.1:3001;
    }
}

server {
    listen 80;
    server_name zxsz007.cn www.zxsz007.cn;
    return 301 https://$host$request_uri;
}
```

### 3.2 环境变量

```bash
# /root/danci007web/.env
PORT=3001
ADMIN_PASSWORD=danci123
CORS_ORIGIN=https://zxsz007.cn
DATA_DIR=./backend/data
UPLOAD_DIR=./backend/uploads
```

### 3.3 PM2 配置

```bash
# 启动
pm2 start ecosystem.config.cjs --env production
pm2 save
pm2 startup

# 查看状态
pm2 status
pm2 logs danci007web
```

---

## 4. 备份

```bash
# 备份数据库和上传文件
tar -czf backup-$(date +%Y%m%d).tar.gz backend/data backend/uploads

# 恢复
tar -xzf backup-20260101.tar.gz
pm2 restart danci007web
```

---

## 5. 常见问题

### Q: 服务无法启动
```bash
pm2 logs danci007web --lines 50
```

### Q: 数据库锁定
```bash
# 停止服务
pm2 stop danci007web

# 删除锁文件
rm -f backend/data/danci007.db-shm backend/data/danci007.db-wal

# 重启
pm2 start danci007web
```

### Q: 更新后样式不对
```bash
# 清除浏览器缓存，或强制刷新
# Ctrl+Shift+R (Windows) / Cmd+Shift+R (Mac)
```
